const {print_to_rows, PI, get_rows, get_lines} = require("./module");

let line = "Пермский государственный сельхоз универ";
// print_to_rows(line);
// console.log(get_rows(line));
console.log(get_lines(line));

console.log(`Pi = ${PI}`);
